plugins {
    id 'java'
    id 'java-library'
    id 'maven-publish'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.8.1'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.8.1'

}

test {
    useJUnitPlatform()
}
jar {
    manifest {
        attributes 'Main-Class': 'com.foo.bar.MainClass'
    }
}
apply plugin: 'maven-publish'

version = '1.1.0'


publishing {
    publications {
        maven(MavenPublication) {
            artifact("build/libs/WellBeingApiGradle-1.1.0.jar") {

                extension "jar"

            }
//            from components.java
//            groupId = 'org.example'
//            artifactId = 'WellBeingApiGradle'
//            version = '1.1.0'
        }
    }

    repositories {
        maven {
            allowInsecureProtocol true
            name = 'nexus'
            url 'http://192.168.1.106:8081/repository/repository_nexus'
            credentials {

                username "admin"

                password "viner30032002"

            }

        }
    }

}

